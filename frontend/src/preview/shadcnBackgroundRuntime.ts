/**
 * @deprecated Legacy CSS background stubs removed — use ReactBits library via react-bits-preview.iife.js.
 * Kept as empty export so imports do not break during transition.
 */
export function getShadcnBackgroundRuntimeScript(): string {
  return ''
}
